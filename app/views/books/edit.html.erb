<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>

  <h4>Title</h4>
  <%= f.text.field :title %>
  
  <h4>Body</h4>
  <%= f.text.area :body %>
  
  <%= f.submit 'Update Book' %>
  
<% end %>
